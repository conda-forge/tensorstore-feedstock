--- a/third_party/aws_c_common/aws_c_common.BUILD.bazel
+++ b/third_party/aws_c_common/aws_c_common.BUILD.bazel
@@ -33,9 +33,6 @@
         "AWS_PTHREAD_SETNAME_TAKES_2ARGS",
         "HAVE_SYSCONF",
     ],
-}) + select({
-    ":have_avx2_intrinsics": ["USE_SIMD_ENCODING"],
-    "//conditions:default": [],
 })
 
 _SRCS = glob([
@@ -59,23 +56,13 @@
         "source/platform_fallback_stubs/system_info.c",
     ],
 }) + select({
-    "@platforms//cpu:x86_64": [
-        "source/arch/intel/cpuid.c",
-    ],
-    "@platforms//cpu:arm64": [],
     "//conditions:default": glob(["source/arch/generic/*.c"]),
 }) + select({
     ":windows_arm64": glob(["source/arch/arm/windows/*.c"]),
     ":macos_arm64": glob(["source/arch/arm/darwin/*.c"]),
     ":linux_arm64": glob(["source/arch/arm/auxv/*.c"]),
     "//conditions:default": [],
-}) + select({
-    ":x86_64_mingw_gcc": glob(["source/arch/intel/asm/*.c"]),
-    ":x86_64_msvc": glob(["source/arch/intel/msvc/*.c"]),
-    ":x86_64_gcc_or_clang": glob(["source/arch/intel/asm/*.c"]),
-    "//conditions:default": [],
 })
-# Note: encoding_avx2.c moved to separate target to ensure AVX2 flags are properly applied
 
 _X86_GCC_COPTS = [
     "-mavx",
@@ -85,9 +72,6 @@
 ]
 
 _COPTS = select({
-    ":x86_64_msvc": ["/arch:AVX2"],
-    ":x86_64_gcc_or_clang": _X86_GCC_COPTS,
-    ":x86_64_mingw_gcc": _X86_GCC_COPTS,
     ":arm64_msvc": [],
     ":arm64_gcc_or_clang": [
         "-march=armv8-a+crc+crypto",
@@ -96,40 +80,6 @@
     "//conditions:default": [],
 })
 
-# Separate library for AVX2 encoding to ensure proper compiler flags
-cc_library(
-    name = "encoding_avx2",
-    srcs = select({
-        ":have_avx2_intrinsics": ["source/arch/intel/encoding_avx2.c"],
-        "//conditions:default": [],
-    }),
-    hdrs = glob([
-        "include/aws/common/*.h",
-        "include/aws/common/private/*.h",
-    ]) + [
-        "include/aws/common/config.h",
-    ],
-    copts = select({
-        ":x86_64_msvc": ["/arch:AVX2"],
-        ":x86_64_gcc_or_clang": ["-mavx", "-mavx2", "-mpclmul", "-msse4.2"],
-        ":x86_64_mingw_gcc": ["-mavx", "-mavx2", "-mpclmul", "-msse4.2"],
-        ":arm64_msvc": [],
-        ":arm64_gcc_or_clang": ["-march=armv8-a+crc+crypto", "-mtune=neoverse-v1"],
-        "//conditions:default": ["-mavx", "-mavx2", "-mpclmul", "-msse4.2"],  # Default to x86 AVX2 flags
-    }),
-    defines = [
-        "INTEL_NO_ITTNOTIFY_API",
-    ],
-    includes = [
-        "include",
-    ],
-    local_defines = _LOCAL_DEFINES,
-    textual_hdrs = glob([
-        "include/**/*.inl",
-    ]),
-    alwayslink = 1,
-)
-
 cc_library(
     name = "aws_c_common",
     srcs = _SRCS,
@@ -179,10 +129,7 @@
     deps = [
         ":external_cjson",
         ":external_libcbor",
-    ] + select({
-        ":have_avx2_intrinsics": [":encoding_avx2"],
-        "//conditions:default": [],
-    }),
+    ],
 )
 
 cc_library(
